import type { ProjectStatus } from '../../../ipc/types';
export function StatusDetails({ status }: { status: ProjectStatus }) { return <div><p>{status.runtime.runningContainerCount}/{status.runtime.containerCount} running</p><p>Definition: {status.definition.state}; Services: {status.definition.serviceCount ?? 'unknown'}</p><p>Observed: {status.runtime.observedAt ?? 'unknown'}</p>{status.issues.length ? <div role="alert" aria-label="Status issues"><ul>{status.issues.map(issue => <li key={`${issue.field ?? 'status'}:${issue.message}`}>{issue.message}</li>)}</ul></div> : null}</div>; }
